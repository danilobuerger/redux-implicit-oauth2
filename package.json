{
  "name": "redux-implicit-oauth2",
  "version": "1.2.2",
  "description": "OAuth 2.0 Implicit Grant Flow with Redux",
  "keywords": [
    "react",
    "redux",
    "implicit",
    "oauth2",
    "auth"
  ],
  "author": "Danilo Bürger <info@danilobuerger.de> (http://danilobuerger.de)",
  "license": "MIT",
  "homepage": "https://github.com/danilobuerger/redux-implicit-oauth2#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/danilobuerger/redux-implicit-oauth2.git"
  },
  "bugs": {
    "url": "https://github.com/danilobuerger/redux-implicit-oauth2/issues"
  },
  "main": "./lib/index.js",
  "files": [
    "dist",
    "lib",
    "src"
  ],
  "dependencies": {
    "cuid": "^1.3.8",
    "query-string": "^5.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "babel-loader": "^8.0.5",
    "check-es3-syntax-cli": "^0.2.1",
    "coveralls": "^3.0.3",
    "cross-env": "^5.0.5",
    "eslint": "^5.16.0",
    "eslint-plugin-jest": "^21.2.0",
    "jest": "^24.5.0",
    "jest-localstorage-mock": "^2.0.1",
    "rimraf": "^2.5.4",
    "webpack": "^4.29.6",
    "webpack-cli": "^3.3.0"
  },
  "scripts": {
    "clean": "rimraf lib dist",
    "lint": "eslint src",
    "build:lib": "babel src --out-dir lib --ignore '**/*.test.js'",
    "build:umd": "cross-env NODE_ENV=development webpack src/index.js --output dist/redux-implicit-oauth2.js",
    "build:umd:min": "cross-env NODE_ENV=production webpack src/index.js --output dist/redux-implicit-oauth2.min.js",
    "build": "npm run build:lib && npm run build:umd && npm run build:umd:min",
    "test": "jest --coverage",
    "coveralls": "cat ./coverage/lcov.info | coveralls",
    "prepublishOnly": "npm run clean && npm run lint && npm run build"
  },
  "jest": {
    "setupFiles": [
      "jest-localstorage-mock"
    ],
    "collectCoverageFrom": [
      "src/**/*.js"
    ]
  },
  "directories": {
    "lib": "lib"
  }
}
